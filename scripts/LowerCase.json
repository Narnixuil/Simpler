{
  "name": "Lower Case",
  "icon": "🔡",
  "description": "Convert selected text to lower case",
  "steps": [
    {
      "codeLines": [
        "if (string.IsNullOrEmpty(context.SelectedText)) {",
        "    context.Notify(\"No selected text detected\");",
        "    return;",
        "}",
        "context.SetOutputText(context.SelectedText.ToLower());"
      ],
      "op": "csharp"
    }
  ]
}
