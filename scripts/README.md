# Scripts Library

This folder is the script library for Simpler.

- Each card maps to one `.json` file.
- Scripts are loaded at runtime from this folder.
- You can add, remove, or edit scripts without rebuilding the app.

If you share a release zip, keep `scripts/` next to `Simpler.exe`.
